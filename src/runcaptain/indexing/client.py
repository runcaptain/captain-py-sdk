# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.index_job_response_v2 import IndexJobResponseV2
from .raw_client import AsyncRawIndexingClient, RawIndexingClient
from .types.index_azure_directory_request_v2processing_type import IndexAzureDirectoryRequestV2ProcessingType
from .types.index_azure_file_request_v2processing_type import IndexAzureFileRequestV2ProcessingType
from .types.index_azure_request_v2processing_type import IndexAzureRequestV2ProcessingType
from .types.index_gcs_directory_request_v2processing_type import IndexGcsDirectoryRequestV2ProcessingType
from .types.index_gcs_file_request_v2processing_type import IndexGcsFileRequestV2ProcessingType
from .types.index_gcs_request_v2processing_type import IndexGcsRequestV2ProcessingType
from .types.index_s3directory_request_v2processing_type import IndexS3DirectoryRequestV2ProcessingType
from .types.index_s3file_request_v2processing_type import IndexS3FileRequestV2ProcessingType
from .types.index_s3request_v2processing_type import IndexS3RequestV2ProcessingType

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class IndexingClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawIndexingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawIndexingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawIndexingClient
        """
        return self._raw_client

    def index_s3bucket_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        aws_access_key_id: str,
        aws_secret_access_key: str,
        processing_type: IndexS3RequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        bucket_region: typing.Optional[str] = OMIT,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from an S3 bucket into a collection. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the S3 bucket

        aws_access_key_id : str
            AWS access key ID with read access to the bucket

        aws_secret_access_key : str
            AWS secret access key

        processing_type : IndexS3RequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        bucket_region : typing.Optional[str]
            AWS region where the bucket is located

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        from runcaptain import Captain

        client = Captain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )
        client.indexing.index_s3bucket_v2(
            collection_name="my_documents",
            bucket_name="my-s3-bucket",
            aws_access_key_id="AKIAIOSFODNN7EXAMPLE",
            aws_secret_access_key="your_secret_key",
            bucket_region="us-east-1",
            processing_type="advanced",
        )
        """
        _response = self._raw_client.index_s3bucket_v2(
            collection_name,
            bucket_name=bucket_name,
            aws_access_key_id=aws_access_key_id,
            aws_secret_access_key=aws_secret_access_key,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            bucket_region=bucket_region,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    def index_s3file_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        file_uri: str,
        aws_access_key_id: str,
        aws_secret_access_key: str,
        processing_type: IndexS3FileRequestV2ProcessingType,
        bucket_region: typing.Optional[str] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index a single file from an S3 bucket into a collection. Returns a job_id for tracking progress.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the S3 bucket

        file_uri : str
            S3 URI format: s3://bucket-name/path/to/file.pdf

        aws_access_key_id : str
            AWS access key ID with read access to the bucket

        aws_secret_access_key : str
            AWS secret access key

        processing_type : IndexS3FileRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        bucket_region : typing.Optional[str]
            AWS region where the bucket is located

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all chunks from this file. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        from runcaptain import Captain

        client = Captain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )
        client.indexing.index_s3file_v2(
            collection_name="my_documents",
            bucket_name="my-s3-bucket",
            file_uri="s3://my-s3-bucket/contracts/acme_contract.pdf",
            aws_access_key_id="AKIAIOSFODNN7EXAMPLE",
            aws_secret_access_key="your_secret_key",
            bucket_region="us-east-1",
            processing_type="advanced",
        )
        """
        _response = self._raw_client.index_s3file_v2(
            collection_name,
            bucket_name=bucket_name,
            file_uri=file_uri,
            aws_access_key_id=aws_access_key_id,
            aws_secret_access_key=aws_secret_access_key,
            processing_type=processing_type,
            bucket_region=bucket_region,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    def index_gcs_bucket_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        service_account_json: str,
        processing_type: IndexGcsRequestV2ProcessingType,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from a Google Cloud Storage bucket into a collection. Returns a job_id for tracking progress.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the GCS bucket

        service_account_json : str
            GCP service account JSON key with read access to the bucket

        processing_type : IndexGcsRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        from runcaptain import Captain

        client = Captain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )
        client.indexing.index_gcs_bucket_v2(
            collection_name="my_documents",
            bucket_name="my-gcs-bucket",
            service_account_json='{"type":"service_account","project_id":"my-project",...}',
            processing_type="advanced",
        )
        """
        _response = self._raw_client.index_gcs_bucket_v2(
            collection_name,
            bucket_name=bucket_name,
            service_account_json=service_account_json,
            processing_type=processing_type,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    def index_gcs_file_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        file_uri: str,
        service_account_json: str,
        processing_type: IndexGcsFileRequestV2ProcessingType,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index a single file from a GCS bucket into a collection. Returns a job_id for tracking progress.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the GCS bucket

        file_uri : str
            GCS URI format: gs://bucket-name/path/to/file.pdf

        service_account_json : str
            GCP service account JSON key with read access to the bucket

        processing_type : IndexGcsFileRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all chunks from this file. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        from runcaptain import Captain

        client = Captain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )
        client.indexing.index_gcs_file_v2(
            collection_name="collection_name",
            bucket_name="my-company-docs",
            file_uri="gs://my-company-docs/contracts/acme_contract.pdf",
            service_account_json='{"type":"service_account","project_id":"my-project",...}',
            processing_type="advanced",
        )
        """
        _response = self._raw_client.index_gcs_file_v2(
            collection_name,
            bucket_name=bucket_name,
            file_uri=file_uri,
            service_account_json=service_account_json,
            processing_type=processing_type,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    def index_s3directory_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        directory_path: str,
        aws_access_key_id: str,
        aws_secret_access_key: str,
        processing_type: IndexS3DirectoryRequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        bucket_region: typing.Optional[str] = OMIT,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from a specific directory in an S3 bucket into a collection. Uses prefix-based filtering to index only files within the specified directory path. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the S3 bucket

        directory_path : str
            Path to the directory within the bucket. Accepts either a relative path (e.g., 'reports/2024/january') or a full S3 URI (e.g., 's3://my-bucket/reports/2024/january'). All files within this directory and its subdirectories will be indexed.

        aws_access_key_id : str
            AWS access key ID with read access to the bucket

        aws_secret_access_key : str
            AWS secret access key

        processing_type : IndexS3DirectoryRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        bucket_region : typing.Optional[str]
            AWS region where the bucket is located

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        from runcaptain import Captain

        client = Captain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )
        client.indexing.index_s3directory_v2(
            collection_name="my_documents",
            bucket_name="my-s3-bucket",
            directory_path="reports/2024/january",
            aws_access_key_id="AKIAIOSFODNN7EXAMPLE",
            aws_secret_access_key="your_secret_key",
            bucket_region="us-east-1",
            processing_type="advanced",
        )
        """
        _response = self._raw_client.index_s3directory_v2(
            collection_name,
            bucket_name=bucket_name,
            directory_path=directory_path,
            aws_access_key_id=aws_access_key_id,
            aws_secret_access_key=aws_secret_access_key,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            bucket_region=bucket_region,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    def index_gcs_directory_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        directory_path: str,
        service_account_json: str,
        processing_type: IndexGcsDirectoryRequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from a specific directory in a GCS bucket into a collection. Uses prefix-based filtering to index only files within the specified directory path. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the GCS bucket

        directory_path : str
            Path to the directory within the bucket. Accepts either a relative path (e.g., 'reports/2024/january') or a full GCS URI (e.g., 'gs://my-bucket/reports/2024/january'). All files within this directory and its subdirectories will be indexed.

        service_account_json : str
            GCP service account JSON key with read access to the bucket

        processing_type : IndexGcsDirectoryRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        from runcaptain import Captain

        client = Captain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )
        client.indexing.index_gcs_directory_v2(
            collection_name="my_documents",
            bucket_name="my-gcs-bucket",
            directory_path="reports/2024/january",
            service_account_json='{"type":"service_account","project_id":"my-project",...}',
            processing_type="advanced",
        )
        """
        _response = self._raw_client.index_gcs_directory_v2(
            collection_name,
            bucket_name=bucket_name,
            directory_path=directory_path,
            service_account_json=service_account_json,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    def index_azure_container_v2(
        self,
        collection_name: str,
        *,
        container_name: str,
        account_name: str,
        account_key: str,
        processing_type: IndexAzureRequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from an Azure Blob Storage container into a collection. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        container_name : str
            Name of the Azure Blob Storage container

        account_name : str
            Azure Storage account name

        account_key : str
            Azure Storage account key (base64-encoded)

        processing_type : IndexAzureRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        from runcaptain import Captain

        client = Captain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )
        client.indexing.index_azure_container_v2(
            collection_name="my_documents",
            container_name="my-container",
            account_name="mystorageaccount",
            account_key="your_account_key",
            processing_type="advanced",
        )
        """
        _response = self._raw_client.index_azure_container_v2(
            collection_name,
            container_name=container_name,
            account_name=account_name,
            account_key=account_key,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    def index_azure_file_v2(
        self,
        collection_name: str,
        *,
        container_name: str,
        file_uri: str,
        account_name: str,
        account_key: str,
        processing_type: IndexAzureFileRequestV2ProcessingType,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index a single file from an Azure Blob Storage container into a collection. Returns a job_id for tracking progress.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        container_name : str
            Name of the Azure Blob Storage container

        file_uri : str
            Azure Blob Storage URI format: https://{account}.blob.core.windows.net/{container}/path/to/file.pdf

        account_name : str
            Azure Storage account name

        account_key : str
            Azure Storage account key (base64-encoded)

        processing_type : IndexAzureFileRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all chunks from this file. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        from runcaptain import Captain

        client = Captain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )
        client.indexing.index_azure_file_v2(
            collection_name="my_documents",
            container_name="my-container",
            file_uri="https://mystorageaccount.blob.core.windows.net/my-container/contracts/acme_contract.pdf",
            account_name="mystorageaccount",
            account_key="your_account_key",
            processing_type="advanced",
        )
        """
        _response = self._raw_client.index_azure_file_v2(
            collection_name,
            container_name=container_name,
            file_uri=file_uri,
            account_name=account_name,
            account_key=account_key,
            processing_type=processing_type,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    def index_azure_directory_v2(
        self,
        collection_name: str,
        *,
        container_name: str,
        directory_path: str,
        account_name: str,
        account_key: str,
        processing_type: IndexAzureDirectoryRequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from a specific directory (prefix) in an Azure Blob Storage container into a collection. Uses prefix-based filtering to index only blobs within the specified path. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        container_name : str
            Name of the Azure Blob Storage container

        directory_path : str
            Path to the directory (prefix) within the container. Accepts either a relative path (e.g., 'reports/2024/january') or a full Azure Blob URI (e.g., 'https://account.blob.core.windows.net/container/reports/2024/january'). All blobs within this prefix will be indexed.

        account_name : str
            Azure Storage account name

        account_key : str
            Azure Storage account key (base64-encoded)

        processing_type : IndexAzureDirectoryRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        from runcaptain import Captain

        client = Captain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )
        client.indexing.index_azure_directory_v2(
            collection_name="my_documents",
            container_name="my-container",
            directory_path="reports/2024/january",
            account_name="mystorageaccount",
            account_key="your_account_key",
            processing_type="advanced",
        )
        """
        _response = self._raw_client.index_azure_directory_v2(
            collection_name,
            container_name=container_name,
            directory_path=directory_path,
            account_name=account_name,
            account_key=account_key,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data


class AsyncIndexingClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawIndexingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawIndexingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawIndexingClient
        """
        return self._raw_client

    async def index_s3bucket_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        aws_access_key_id: str,
        aws_secret_access_key: str,
        processing_type: IndexS3RequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        bucket_region: typing.Optional[str] = OMIT,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from an S3 bucket into a collection. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the S3 bucket

        aws_access_key_id : str
            AWS access key ID with read access to the bucket

        aws_secret_access_key : str
            AWS secret access key

        processing_type : IndexS3RequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        bucket_region : typing.Optional[str]
            AWS region where the bucket is located

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        import asyncio

        from runcaptain import AsyncCaptain

        client = AsyncCaptain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )


        async def main() -> None:
            await client.indexing.index_s3bucket_v2(
                collection_name="my_documents",
                bucket_name="my-s3-bucket",
                aws_access_key_id="AKIAIOSFODNN7EXAMPLE",
                aws_secret_access_key="your_secret_key",
                bucket_region="us-east-1",
                processing_type="advanced",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.index_s3bucket_v2(
            collection_name,
            bucket_name=bucket_name,
            aws_access_key_id=aws_access_key_id,
            aws_secret_access_key=aws_secret_access_key,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            bucket_region=bucket_region,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    async def index_s3file_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        file_uri: str,
        aws_access_key_id: str,
        aws_secret_access_key: str,
        processing_type: IndexS3FileRequestV2ProcessingType,
        bucket_region: typing.Optional[str] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index a single file from an S3 bucket into a collection. Returns a job_id for tracking progress.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the S3 bucket

        file_uri : str
            S3 URI format: s3://bucket-name/path/to/file.pdf

        aws_access_key_id : str
            AWS access key ID with read access to the bucket

        aws_secret_access_key : str
            AWS secret access key

        processing_type : IndexS3FileRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        bucket_region : typing.Optional[str]
            AWS region where the bucket is located

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all chunks from this file. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        import asyncio

        from runcaptain import AsyncCaptain

        client = AsyncCaptain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )


        async def main() -> None:
            await client.indexing.index_s3file_v2(
                collection_name="my_documents",
                bucket_name="my-s3-bucket",
                file_uri="s3://my-s3-bucket/contracts/acme_contract.pdf",
                aws_access_key_id="AKIAIOSFODNN7EXAMPLE",
                aws_secret_access_key="your_secret_key",
                bucket_region="us-east-1",
                processing_type="advanced",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.index_s3file_v2(
            collection_name,
            bucket_name=bucket_name,
            file_uri=file_uri,
            aws_access_key_id=aws_access_key_id,
            aws_secret_access_key=aws_secret_access_key,
            processing_type=processing_type,
            bucket_region=bucket_region,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    async def index_gcs_bucket_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        service_account_json: str,
        processing_type: IndexGcsRequestV2ProcessingType,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from a Google Cloud Storage bucket into a collection. Returns a job_id for tracking progress.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the GCS bucket

        service_account_json : str
            GCP service account JSON key with read access to the bucket

        processing_type : IndexGcsRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        import asyncio

        from runcaptain import AsyncCaptain

        client = AsyncCaptain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )


        async def main() -> None:
            await client.indexing.index_gcs_bucket_v2(
                collection_name="my_documents",
                bucket_name="my-gcs-bucket",
                service_account_json='{"type":"service_account","project_id":"my-project",...}',
                processing_type="advanced",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.index_gcs_bucket_v2(
            collection_name,
            bucket_name=bucket_name,
            service_account_json=service_account_json,
            processing_type=processing_type,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    async def index_gcs_file_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        file_uri: str,
        service_account_json: str,
        processing_type: IndexGcsFileRequestV2ProcessingType,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index a single file from a GCS bucket into a collection. Returns a job_id for tracking progress.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the GCS bucket

        file_uri : str
            GCS URI format: gs://bucket-name/path/to/file.pdf

        service_account_json : str
            GCP service account JSON key with read access to the bucket

        processing_type : IndexGcsFileRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all chunks from this file. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        import asyncio

        from runcaptain import AsyncCaptain

        client = AsyncCaptain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )


        async def main() -> None:
            await client.indexing.index_gcs_file_v2(
                collection_name="collection_name",
                bucket_name="my-company-docs",
                file_uri="gs://my-company-docs/contracts/acme_contract.pdf",
                service_account_json='{"type":"service_account","project_id":"my-project",...}',
                processing_type="advanced",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.index_gcs_file_v2(
            collection_name,
            bucket_name=bucket_name,
            file_uri=file_uri,
            service_account_json=service_account_json,
            processing_type=processing_type,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    async def index_s3directory_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        directory_path: str,
        aws_access_key_id: str,
        aws_secret_access_key: str,
        processing_type: IndexS3DirectoryRequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        bucket_region: typing.Optional[str] = OMIT,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from a specific directory in an S3 bucket into a collection. Uses prefix-based filtering to index only files within the specified directory path. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the S3 bucket

        directory_path : str
            Path to the directory within the bucket. Accepts either a relative path (e.g., 'reports/2024/january') or a full S3 URI (e.g., 's3://my-bucket/reports/2024/january'). All files within this directory and its subdirectories will be indexed.

        aws_access_key_id : str
            AWS access key ID with read access to the bucket

        aws_secret_access_key : str
            AWS secret access key

        processing_type : IndexS3DirectoryRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        bucket_region : typing.Optional[str]
            AWS region where the bucket is located

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        import asyncio

        from runcaptain import AsyncCaptain

        client = AsyncCaptain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )


        async def main() -> None:
            await client.indexing.index_s3directory_v2(
                collection_name="my_documents",
                bucket_name="my-s3-bucket",
                directory_path="reports/2024/january",
                aws_access_key_id="AKIAIOSFODNN7EXAMPLE",
                aws_secret_access_key="your_secret_key",
                bucket_region="us-east-1",
                processing_type="advanced",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.index_s3directory_v2(
            collection_name,
            bucket_name=bucket_name,
            directory_path=directory_path,
            aws_access_key_id=aws_access_key_id,
            aws_secret_access_key=aws_secret_access_key,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            bucket_region=bucket_region,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    async def index_gcs_directory_v2(
        self,
        collection_name: str,
        *,
        bucket_name: str,
        directory_path: str,
        service_account_json: str,
        processing_type: IndexGcsDirectoryRequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from a specific directory in a GCS bucket into a collection. Uses prefix-based filtering to index only files within the specified directory path. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        bucket_name : str
            Name of the GCS bucket

        directory_path : str
            Path to the directory within the bucket. Accepts either a relative path (e.g., 'reports/2024/january') or a full GCS URI (e.g., 'gs://my-bucket/reports/2024/january'). All files within this directory and its subdirectories will be indexed.

        service_account_json : str
            GCP service account JSON key with read access to the bucket

        processing_type : IndexGcsDirectoryRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        import asyncio

        from runcaptain import AsyncCaptain

        client = AsyncCaptain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )


        async def main() -> None:
            await client.indexing.index_gcs_directory_v2(
                collection_name="my_documents",
                bucket_name="my-gcs-bucket",
                directory_path="reports/2024/january",
                service_account_json='{"type":"service_account","project_id":"my-project",...}',
                processing_type="advanced",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.index_gcs_directory_v2(
            collection_name,
            bucket_name=bucket_name,
            directory_path=directory_path,
            service_account_json=service_account_json,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    async def index_azure_container_v2(
        self,
        collection_name: str,
        *,
        container_name: str,
        account_name: str,
        account_key: str,
        processing_type: IndexAzureRequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from an Azure Blob Storage container into a collection. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        container_name : str
            Name of the Azure Blob Storage container

        account_name : str
            Azure Storage account name

        account_key : str
            Azure Storage account key (base64-encoded)

        processing_type : IndexAzureRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        import asyncio

        from runcaptain import AsyncCaptain

        client = AsyncCaptain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )


        async def main() -> None:
            await client.indexing.index_azure_container_v2(
                collection_name="my_documents",
                container_name="my-container",
                account_name="mystorageaccount",
                account_key="your_account_key",
                processing_type="advanced",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.index_azure_container_v2(
            collection_name,
            container_name=container_name,
            account_name=account_name,
            account_key=account_key,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    async def index_azure_file_v2(
        self,
        collection_name: str,
        *,
        container_name: str,
        file_uri: str,
        account_name: str,
        account_key: str,
        processing_type: IndexAzureFileRequestV2ProcessingType,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index a single file from an Azure Blob Storage container into a collection. Returns a job_id for tracking progress.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        container_name : str
            Name of the Azure Blob Storage container

        file_uri : str
            Azure Blob Storage URI format: https://{account}.blob.core.windows.net/{container}/path/to/file.pdf

        account_name : str
            Azure Storage account name

        account_key : str
            Azure Storage account key (base64-encoded)

        processing_type : IndexAzureFileRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all chunks from this file. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        import asyncio

        from runcaptain import AsyncCaptain

        client = AsyncCaptain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )


        async def main() -> None:
            await client.indexing.index_azure_file_v2(
                collection_name="my_documents",
                container_name="my-container",
                file_uri="https://mystorageaccount.blob.core.windows.net/my-container/contracts/acme_contract.pdf",
                account_name="mystorageaccount",
                account_key="your_account_key",
                processing_type="advanced",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.index_azure_file_v2(
            collection_name,
            container_name=container_name,
            file_uri=file_uri,
            account_name=account_name,
            account_key=account_key,
            processing_type=processing_type,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data

    async def index_azure_directory_v2(
        self,
        collection_name: str,
        *,
        container_name: str,
        directory_path: str,
        account_name: str,
        account_key: str,
        processing_type: IndexAzureDirectoryRequestV2ProcessingType,
        idempotency_key: typing.Optional[str] = None,
        max_files: typing.Optional[int] = OMIT,
        skip_existing: typing.Optional[bool] = OMIT,
        custom_metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IndexJobResponseV2:
        """
        Index all files from a specific directory (prefix) in an Azure Blob Storage container into a collection. Uses prefix-based filtering to index only blobs within the specified path. Returns a job_id for tracking progress via GET /v2/jobs/{job_id}.

        Parameters
        ----------
        collection_name : str
            Name of the collection to index into

        container_name : str
            Name of the Azure Blob Storage container

        directory_path : str
            Path to the directory (prefix) within the container. Accepts either a relative path (e.g., 'reports/2024/january') or a full Azure Blob URI (e.g., 'https://account.blob.core.windows.net/container/reports/2024/january'). All blobs within this prefix will be indexed.

        account_name : str
            Azure Storage account name

        account_key : str
            Azure Storage account key (base64-encoded)

        processing_type : IndexAzureDirectoryRequestV2ProcessingType
            Document processing type. 'advanced' uses agentic OCR with AI-enhanced extraction for complex layouts, tables, figures, charts, and documents containing images. 'basic' provides reliable OCR optimized for general document indexing and high-volume processing.

        idempotency_key : typing.Optional[str]
            UUID for request deduplication

        max_files : typing.Optional[int]
            Maximum number of files to index (optional)

        skip_existing : typing.Optional[bool]
            Skip files that are already indexed in the collection. When true, only new files will be indexed. Set to false to re-index all files.

        custom_metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom metadata to attach to all indexed chunks. Keys must be strings. Values: str, int, float, bool, or array of strings.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IndexJobResponseV2
            Indexing Job Started

        Examples
        --------
        import asyncio

        from runcaptain import AsyncCaptain

        client = AsyncCaptain(
            authorization="YOUR_AUTHORIZATION",
            organization_id="YOUR_ORGANIZATION_ID",
        )


        async def main() -> None:
            await client.indexing.index_azure_directory_v2(
                collection_name="my_documents",
                container_name="my-container",
                directory_path="reports/2024/january",
                account_name="mystorageaccount",
                account_key="your_account_key",
                processing_type="advanced",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.index_azure_directory_v2(
            collection_name,
            container_name=container_name,
            directory_path=directory_path,
            account_name=account_name,
            account_key=account_key,
            processing_type=processing_type,
            idempotency_key=idempotency_key,
            max_files=max_files,
            skip_existing=skip_existing,
            custom_metadata=custom_metadata,
            request_options=request_options,
        )
        return _response.data
